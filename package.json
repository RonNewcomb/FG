{
  "name": "fg",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "typescript": "^4.0.3"
  },
  "scripts": {
    "start": "http://localhost/FG",
    "build, test, deploy": "tsc && jest && cmd /c building.bat",
    "build & deploy": "tsc && cmd /c building.bat",
    "build & test": "tsc && jest",
    "build": "tsc",
    "test": "jest",
    "deploy": "cmd /c building.bat",
    "view coverage": "C:/code/FG/coverage/lcov-report/index.html"
  },
  "devDependencies": {
    "@babel/core": "^7.15.5",
    "@babel/preset-env": "^7.15.6",
    "@babel/preset-typescript": "^7.15.0",
    "@types/jest": "^27.0.1",
    "babel-jest": "^27.2.0",
    "jest": "^27.2.0",
    "ts-node": "^10.2.1"
  }
}
